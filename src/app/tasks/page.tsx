import { ModulePlaceholder } from "@/components/ModulePlaceholder";

export default function TasksPage() {
  return (
    <ModulePlaceholder
      eyebrow="Ward worklist"
      title="Tasks"
      description="Ward-wide care tasks across every patient."
      phase="Phase 6"
    />
  );
}
