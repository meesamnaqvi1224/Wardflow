# Copy to keystore.properties and fill in real values.
# NEVER commit keystore.properties or *.keystore / *.jks

storeFile=wardflow-release.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=wardflow
keyPassword=YOUR_KEY_PASSWORD
